/**
 * Обогащённый контент для 15 скиллов (Tools коллекция)
 * Используется скриптом enrich-tools.ts для обновления description, seoTitle, seoDescription
 */

import {
  heading,
  paragraph,
  list,
  quote,
  codeBlock,
  hr,
  root,
} from "../lexical";

export interface EnrichedContent {
  slug: string;
  description: ReturnType<typeof root>;
  seoTitle: string;
  seoDescription: string;
}

export const enrichedSkills: EnrichedContent[] = [
  /* ─────────────────────────────────────────────
     1. aiogram-bot
  ───────────────────────────────────────────── */
  {
    slug: "aiogram-bot",
    description: root([
      paragraph(
        "Скилл для создания Telegram-ботов на **Aiogram v3 + Python**. Описываешь задачу — получаешь готовый проект: хендлеры, FSM-диалоги, клавиатуры, БД, Docker. Только v3 — v2 не поддерживается.",
      ),

      hr(),
      heading(2, "Что генерирует"),
      list([
        "**Структура проекта** — `handlers/`, `keyboards/`, `states/`, `middlewares/`, `database/`",
        "**FSM-диалоги** — пошаговые формы, регистрация, опросы с валидацией",
        "**Клавиатуры** — inline-кнопки с callback + reply-клавиатуры",
        "**Middleware** — авторизация, rate-limit, логирование",
        "**БД** — SQLAlchemy async (PostgreSQL или SQLite) + Alembic миграции",
        "**Деплой** — `docker-compose.yml`, `.env.example`, Webhook/Long Polling",
        "**Тесты** — pytest для хендлеров и FSM-переходов",
      ]),

      hr(),
      heading(2, "Структура проекта"),
      codeBlock(
        "bot/\n├── main.py              # Точка входа, Dispatcher\n├── config.py            # Pydantic Settings из .env\n├── handlers/\n│   ├── start.py         # /start, /help\n│   ├── booking.py       # FSM-сценарий\n│   └── admin.py         # Управление\n├── keyboards/\n│   ├── inline.py        # InlineKeyboardMarkup\n│   └── reply.py         # ReplyKeyboardMarkup\n├── states/\n│   └── booking.py       # StatesGroup\n├── middlewares/\n│   ├── auth.py          # Проверка ролей\n│   └── throttle.py      # Anti-flood\n├── database/\n│   ├── models.py        # SQLAlchemy ORM\n│   └── queries.py       # CRUD-операции\n├── alembic/             # Миграции БД\n├── tests/\n│   └── test_booking.py\n├── docker-compose.yml\n├── .env.example\n└── requirements.txt",
        "text",
      ),

      hr(),
      heading(2, "Готовые промпты"),
      paragraph("Скопируй и вставь в Claude Code:"),
      codeBlock(
        "Создай Telegram-бота для записи на стрижку.\n\nТребования:\n- 3 мастера, у каждого свой график работы\n- Слоты по 30 минут, бронирование на неделю вперёд\n- FSM-диалог: выбор мастера → дата → время → подтверждение\n- После записи: сообщение с деталями + напоминание за час\n- Админ-команда /bookings — список записей на сегодня\n\nСтек: aiogram v3 + PostgreSQL + Docker\nДеплой: docker-compose на VPS, Webhook через nginx",
        "text",
      ),
      codeBlock(
        "Сделай бота для управления задачами:\n- /add <текст> — создать задачу\n- /list — активные задачи (нумерованный список)\n- /done <id> — отметить выполненной\n- /remind — включить напоминание через 24ч\n\nБД: SQLite (один файл, без Docker).\nДобавь inline-кнопки: ✅ Выполнить | 🗑 Удалить | ⏰ Напомнить",
        "text",
      ),

      hr(),
      heading(2, "Настройка в проекте"),
      paragraph(
        "Чтобы скилл активировался автоматически, укажи стек в `CLAUDE.md`:",
      ),
      codeBlock(
        "## Стек\n- Telegram Bot API: aiogram v3\n- БД: PostgreSQL + SQLAlchemy async\n- Деплой: Docker + Webhook",
        "markdown",
      ),
      paragraph(
        "Без этого Claude всё равно подключит скилл, если в задаче упоминается telegram, бот, aiogram или хендлер.",
      ),

      hr(),
      heading(2, "Заметки"),
      list([
        "**Только Aiogram v3** — у v2 другой API (нет роутеров, другой FSM), миграция обязательна",
        "**БД**: SQLite для простых ботов (<100 юзеров), PostgreSQL для остального",
        "**Docker опционален** — бот запускается и через `python main.py`",
        "**Webhook vs Polling**: Webhook для прода (нужен домен + SSL), Long Polling для разработки",
      ]),
    ]),
    seoTitle: "aiogram-bot — Telegram-боты на Aiogram v3 | StackOverVibe",
    seoDescription:
      "Скилл aiogram-bot: структура проекта, FSM-диалоги, клавиатуры, PostgreSQL, Docker. Готовые промпты для создания Telegram-ботов с Claude Code.",
  },

  /* ─────────────────────────────────────────────
     2. telegram-post-style
  ───────────────────────────────────────────── */
  {
    slug: "telegram-post-style",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Ведёшь канал в Телеграме и тратишь час на форматирование каждого поста? Этот скилл делает это за секунды. Ты даёшь сырой текст — получаешь красиво оформленный пост готовый к публикации.",
      ),
      paragraph(
        "Напиши: «Вот мои заметки про релиз, сделай пост для канала» — и Claude сам расставит акценты, добавит структуру и проверит что всё влезает в лимиты Телеграма.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **telegram-post-style** превращает сырой текст в отформатированный Telegram-пост с нужным стилем. Поддерживает MarkdownV2, HTML и тексты без разметки — Claude выбирает нужный вариант автоматически.",
      ),
      paragraph(
        "Скилл учитывает ограничения Telegram: лимит 4096 символов, запрет на некоторые спецсимволы, поведение превью ссылок.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Нужно написать анонс фичи или релиза в Telegram-канал",
        "Есть сырой технический текст, который надо подать читабельно",
        "Хочешь оформить тред (цепочку постов) по длинной теме",
        "Нужен пост с кнопками (inline keyboard) для Telegram-бота",
        "Форматируешь changelog или список изменений",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude получает сырой контент и целевую аудиторию поста",
          "Определяет тип поста: анонс, обучение, мнение, changelog, тред",
          "Выбирает структуру: хук → суть → детали → CTA",
          "Применяет форматирование: жирный для акцентов, моноширинный для кода, эмодзи по вкусу",
          "Проверяет длину и при необходимости делит на части",
          "Возвращает готовый текст + версию для превью (первые 2 строки)",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      paragraph("Примеры задач для этого скилла:"),
      codeBlock(
        "Напиши Telegram-пост про релиз v2.0 моего бота.\nЧто нового: добавили FSM-диалоги, оплату через ЮKassa, личный кабинет.\nАудитория: пользователи бота, не технари.\nСтиль: энергично, без воды, с эмодзи.\nКанал: @mybotchannel",
        "text",
      ),
      codeBlock(
        "Сделай тред из 3 постов про то, почему вайбкодинг лучше классической разработки.\nПост 1: проблема (долго, дорого, скучно)\nПост 2: решение (Claude + правильные промпты)\nПост 3: призыв попробовать + ссылка\nСтиль: неформальный, с конкретными примерами",
        "text",
      ),

      hr(),
      heading(2, "Пример"),
      codeBlock(
        "# Вход:\nРелиз aiogram-bot v1.2: добавили поддержку платежей Telegram Stars,\nфикс бага с кириллицей в callback_data, новый middleware для логирования.\n\n# Выход (MarkdownV2):\n🚀 *aiogram\\-bot v1\\.2* — горячий релиз\\!\n\n✅ Telegram Stars — принимаем платежи нативно\n🐛 Фикс кириллицы в `callback_data` \\(наконец\\-то\\)\n📋 Middleware логирования — видишь все апдейты в реальном времени\n\nОбновляйся: `pip install aiogram-bot==1.2.0`\n\n👉 [Changelog](https://github.com/...)",
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph(
        "Автоматическая публикация через бота — настрой хук на завершение деплоя:",
      ),
      codeBlock(
        '# .github/workflows/notify.yml\n- name: Notify Telegram\n  run: |\n    curl -s -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \\\n      -d chat_id="$CHANNEL_ID" \\\n      -d parse_mode="MarkdownV2" \\\n      -d text="$(cat release-notes.md)"',
        "bash",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Почему MarkdownV2 такой капризный?"),
      paragraph(
        "Telegram MarkdownV2 требует экранировать **все** спецсимволы: `.`, `!`, `-`, `(`, `)` и другие. Claude делает это автоматически, но если копируешь вручную — проверь через `@MarkdownV2_bot`.",
      ),
      heading(3, "Как сделать пост с кнопками?"),
      paragraph(
        'Укажи в задаче: "добавь inline-кнопки: [Открыть](url), [Подробнее](url)". Claude сгенерирует `reply_markup` для отправки через Bot API.',
      ),
    ]),
    seoTitle:
      "telegram-post-style — Посты для Telegram-каналов | StackOverVibe",
    seoDescription:
      "Скилл для форматирования Telegram-постов: MarkdownV2, HTML, треды, inline-кнопки. Готовые тексты с Claude Code.",
  },

  /* ─────────────────────────────────────────────
     3. new-project
  ───────────────────────────────────────────── */
  {
    slug: "new-project",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Есть идея — и хочется сразу начать делать, а не возиться с настройками. Этот скилл создаёт всю основу проекта за один промпт: папки, конфиги, подключение к базе данных и даже первый коммит в GitHub.",
      ),
      paragraph(
        "Раньше ты тратил день на настройку. Теперь пишешь что хочешь сделать — и через пару минут уже пишешь реальный код, а не разбираешься с окружением.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **new-project** — стартовый пакет для любого нового проекта. Создаёт структуру папок, базовые конфиги, `CLAUDE.md`, `.env.example`, `docker-compose.yml`, GitHub-репо и первый коммит.",
      ),
      paragraph(
        "Один промпт — и ты получаешь рабочий скелет проекта, готовый к разработке. Никаких ручных настроек.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Начинаешь новый проект с нуля (любой стек)",
        "Нужно быстро создать монорепо или микросервис",
        "Хочешь стандартизировать структуру под свой стиль разработки",
        "Клонируешь чужой проект и адаптируешь под себя",
        "Создаёшь шаблонный репозиторий (template repo)",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude задаёт уточняющие вопросы: стек, назначение, деплой-таргет",
          "Генерирует структуру директорий под выбранный фреймворк",
          "Создаёт `CLAUDE.md` с описанием проекта и правилами для AI",
          "Настраивает `.env.example` со всеми нужными переменными",
          "Инициализирует `git init`, делает первый коммит с conventional commit message",
          "Создаёт GitHub-репо через `gh repo create` (опционально)",
          "Настраивает CI/CD: GitHub Actions или Gitea workflow",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      paragraph("Запускай скилл командой `/new-project` или описанием задачи:"),
      codeBlock(
        "/new-project\nНазвание: vibe-tracker\nНазначение: трекер привычек с Telegram-уведомлениями\nСтек: Python + aiogram v3 + PostgreSQL\nДеплой: VPS (Ubuntu 22.04), Docker\nGitHub: да, публичный",
        "text",
      ),
      codeBlock(
        "/new-project\nНазвание: landing-saas\nНазначение: лендинг SaaS-продукта\nСтек: Next.js 14 + TypeScript + Tailwind\nДеплой: Vercel\nCMS: нет, статика",
        "text",
      ),

      hr(),
      heading(2, "Пример вывода"),
      codeBlock(
        "vibe-tracker/\n├── .claude/\n│   └── settings.json     # Хуки Claude Code\n├── bot/\n│   ├── handlers/\n│   ├── keyboards/\n│   └── main.py\n├── database/\n│   └── models.py\n├── tests/\n├── docker-compose.yml\n├── Dockerfile\n├── .env.example\n├── .gitignore\n├── CLAUDE.md             # AI-инструкции для проекта\n├── README.md\n└── pyproject.toml",
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Добавь алиас для быстрого старта любого проекта:"),
      codeBlock(
        '# ~/.bashrc или ~/.zshrc\nalias new-project="claude --dangerously-skip-permissions \\\n  \\"Запусти скилл new-project. Жду вопросов.\\""\n\n# Запуск:\n$ new-project',
        "bash",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Какие стеки поддерживаются?"),
      paragraph(
        "Любые. Скилл адаптирует структуру под стек: **Next.js**, **FastAPI**, **aiogram**, **Express**, **Go**, **Rust** и другие. Просто укажи стек в задаче.",
      ),
      heading(3, "Создаёт ли скилл реальные файлы или только план?"),
      paragraph(
        "Реальные файлы — Claude использует инструменты Write/Bash для создания структуры на диске. В конце делается `git init` и первый коммит.",
      ),
    ]),
    seoTitle: "new-project — Старт проекта за один промпт | StackOverVibe",
    seoDescription:
      "Скилл new-project: структура, CLAUDE.md, Docker, GitHub — полный скелет проекта с нуля. Для любого стека.",
  },

  /* ─────────────────────────────────────────────
     4. project-launcher
  ───────────────────────────────────────────── */
  {
    slug: "project-launcher",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Скачал чужой проект или вернулся к своему после перерыва — и не помнишь как его запустить? Этот скилл разбирается за тебя. Он читает все файлы проекта и сам выполняет нужные команды по порядку.",
      ),
      paragraph(
        "Скажи Claude: «Я в папке проекта, помоги запустить» — и он установит все нужные программы, запустит базу данных и откроет сайт. Никакого гугла, никаких ошибок.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **project-launcher** запускает существующий проект в локальной среде или на сервере: устанавливает зависимости, поднимает Docker-контейнеры, применяет миграции, запускает dev-сервер.",
      ),
      paragraph(
        "Отличается от `new-project`: там создаём с нуля, здесь — **поднимаем уже написанный проект**. Claude читает `README.md`, `docker-compose.yml`, `package.json` и определяет правильный порядок команд.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Клонировал репозиторий и не знаешь с чего начать",
        "Возвращаешься к проекту после долгого перерыва",
        "Поднимаешь проект на новом сервере или VPS",
        "После git pull нужно применить миграции и перезапустить сервисы",
        "Настраиваешь dev-окружение для нового разработчика в команде",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude читает `README.md`, `docker-compose.yml`, `package.json` / `pyproject.toml`",
          "Определяет стек и порядок запуска (БД → бэкенд → фронтенд)",
          "Проверяет `.env` — если нет, создаёт из `.env.example` и просит заполнить",
          "Выполняет установку зависимостей: `npm install`, `pip install`, `go mod download`",
          "Поднимает Docker-контейнеры через `docker compose up -d`",
          "Применяет миграции: `prisma migrate deploy`, `alembic upgrade head`",
          "Запускает dev-сервер и выводит URL для проверки",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Я склонировал проект stackovervibe. Помоги запустить локально.\nОС: macOS, Docker установлен, Node.js 20.\nПорты 3000 и 5432 свободны.",
        "text",
      ),
      codeBlock(
        "Поднимаю проект на новом VPS (Ubuntu 22.04).\nВсё уже склонировано в ~/myproject.\nНужно: установить зависимости, настроить .env, запустить через Docker.",
        "text",
      ),

      hr(),
      heading(2, "Пример сессии запуска"),
      codeBlock(
        '$ claude "Запусти проект, я в папке /home/claude/myproject"\n\n[Claude читает файлы...]\n\n1. Найден docker-compose.yml (app + postgres)\n2. .env отсутствует → создаю из .env.example\n   ! Заполни: DATABASE_URL, SECRET_KEY, BOT_TOKEN\n3. docker compose up -d --build\n4. Жду готовности postgres...\n5. npx prisma migrate deploy\n6. npm run dev\n\n✅ Проект запущен: http://localhost:3000',
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Создай скрипт `launch.sh` в корне проекта:"),
      codeBlock(
        '#!/bin/bash\n# launch.sh — быстрый запуск проекта\nset -e\n\necho "Проверяю .env..."\n[ ! -f .env ] && cp .env.example .env && echo "Заполни .env!" && exit 1\n\necho "Запускаю контейнеры..."\ndocker compose up -d --build\n\necho "Применяю миграции..."\ndocker compose exec app npx prisma migrate deploy\n\necho "✅ Готово: http://localhost:3000"',
        "bash",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Что если не хватает .env переменных?"),
      paragraph(
        "Claude остановится и явно укажет, какие переменные нужно заполнить, и объяснит как их получить (например, токен бота — через @BotFather).",
      ),
      heading(3, "Работает ли без Docker?"),
      paragraph(
        'Да. Если Docker не нужен, Claude запустит сервисы нативно через `npm run dev` или `python main.py`. Просто укажи "без Docker" в задаче.',
      ),
    ]),
    seoTitle:
      "project-launcher — Запуск проекта с нуля за минуты | StackOverVibe",
    seoDescription:
      "Скилл project-launcher: установка зависимостей, Docker, миграции, dev-сервер — поднимаешь любой проект одной командой.",
  },

  /* ─────────────────────────────────────────────
     5. project-knowledge
  ───────────────────────────────────────────── */
  {
    slug: "project-knowledge",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Возвращаешься к проекту через месяц — и тратишь полдня чтобы вспомнить как он устроен. Или объясняешь Claude контекст заново в каждой сессии. Этот скилл создаёт живую документацию, которую AI читает сам.",
      ),
      paragraph(
        "Один раз попроси Claude написать документацию по проекту — и в следующий раз он сразу поймёт что к чему. Как записная книжка, только умнее.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **project-knowledge** создаёт и поддерживает папку `docs/project-knowledge/` — живую документацию проекта для AI-агентов. Четыре файла: `project.md`, `architecture.md`, `patterns.md`, `deployment.md`.",
      ),
      paragraph(
        "Это навигационный гид для Claude: открыл документацию → понял проект без чтения кода. Обновляется после каждой фичи, не устаревает.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Начинаешь новый проект и хочешь с первого дня иметь хорошую документацию",
        "Проект вырос и Claude перестал понимать контекст с первого раза",
        "Возвращаешься к проекту через месяц-другой",
        "Хочешь передать проект другому разработчику (или AI-агенту)",
        "Провести аудит документации — что устарело, что дублируется",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude читает существующий код, `CLAUDE.md`, `package.json`, `docker-compose.yml`",
          "Составляет `project.md`: что за проект, для кого, 3-5 ключевых фич, scope",
          "Составляет `architecture.md`: стек + ПОЧЕМУ, структура, зависимости, модель данных",
          "Составляет `patterns.md`: уникальные паттерны ЭТОГО проекта, git workflow",
          "Составляет `deployment.md`: платформа, env vars, CI/CD, rollback, мониторинг",
          "Обновляет `CLAUDE.md` — ссылки на документацию, ключевые правила",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "/project-knowledge\nСоздай документацию для текущего проекта.\nПрочитай все ключевые файлы и составь 4 документа.",
        "text",
      ),
      codeBlock(
        "Обнови architecture.md — мы добавили Redis для кэширования\nи переехали с REST на tRPC для фронтенд-бэкенд связи.",
        "text",
      ),

      hr(),
      heading(2, "Пример architecture.md"),
      codeBlock(
        "# Архитектура StackOverVibe\n\n## Стек\n- **Next.js 14** — App Router, SSR/SSG (выбрали за SEO)\n- **Payload CMS** — headless CMS, PostgreSQL под капотом\n- **TypeScript** — строгая типизация, 0 any\n- **Tailwind CSS** — утилитарный CSS, дизайн-система\n\n## Структура\nsrc/\n├── app/          # Next.js страницы (App Router)\n├── components/   # React компоненты\n├── collections/  # Payload CMS коллекции\n├── lib/          # Бизнес-логика, утилиты\n└── seed/         # Скрипты наполнения данными\n\n## Ключевые решения\n- Payload CMS вместо Strapi: лучший TypeScript-support\n- App Router вместо Pages: layout.tsx, server components",
        "markdown",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph(
        "Добавь в `CLAUDE.md` правило обновлять документацию после каждой фичи:",
      ),
      codeBlock(
        '## Workflow: после каждого блока\n1. Обнови ПРОГРЕСС в CLAUDE.md\n2. Если изменилась архитектура — обнови architecture.md\n3. Если появились новые паттерны — обнови patterns.md\n4. Коммит: "docs: обновить project-knowledge после [фича]"',
        "markdown",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Документация устаревает — как поддерживать?"),
      paragraph(
        'После каждой значимой фичи добавляй в промпт: "обнови architecture.md". Claude читает изменения в коде и актуализирует документ. Занимает 2-3 минуты.',
      ),
      heading(3, "Можно ли хранить код в документации?"),
      paragraph(
        "Нет — это антипаттерн. Код устаревает и раздувает контекст. Вместо кода — **ссылки на файлы**: `[auth.ts:45-67](src/auth/jwt.ts#L45)`.",
      ),
    ]),
    seoTitle: "project-knowledge — Живая документация для AI | StackOverVibe",
    seoDescription:
      "Скилл project-knowledge: создаёт project.md, architecture.md, patterns.md, deployment.md — документацию которую понимает Claude.",
  },

  /* ─────────────────────────────────────────────
     6. feature-execution
  ───────────────────────────────────────────── */
  {
    slug: "feature-execution",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Дал задачу Claude — и он либо сделал что-то не то, либо потерялся в середине. Этот скилл — как прораб на стройке. Сначала составляет план, согласовывает с тобой, потом делает шаг за шагом и отчитывается после каждого.",
      ),
      paragraph(
        "Ты видишь прогресс, можешь остановить и поправить в любой момент. Большая задача разбивается на маленькие понятные шаги — и ничего не теряется.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **feature-execution** — основной рабочий процесс разработки фичи. Принимает задачу, декомпозирует на блоки, выполняет пошагово с чекпоинтами, тестами и коммитами после каждого блока.",
      ),
      paragraph(
        'Это "мозг" вайбкодинга: планируем → согласовываем → выполняем → тестируем → коммитим. Ни одного лишнего шага.',
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Разрабатываешь новую фичу (M или L размера)",
        "Нужно декомпозировать большую задачу на управляемые блоки",
        "Хочешь видеть прогресс и подтверждать каждый этап",
        "Работаешь над критичным функционалом где нужны тесты",
        "Нужна трассировка: что сделано, почему именно так",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Brainstorming: Claude задаёт уточняющие вопросы (edge cases, стек, ограничения)",
          "Планирование: декомпозиция на блоки с оценкой времени, жду подтверждения",
          "Выполнение: блок за блоком, с маркерами прогресса",
          "Чекпоинт после каждого блока: что сделано, следующий шаг, продолжаем?",
          "Тестирование: пишу тесты сразу в той же сессии",
          "Коммит: conventional commit с описанием изменений",
          "Обновление `CLAUDE.md`: секция ПРОГРЕСС, что выполнено",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "feat: добавить систему уведомлений\n\nКогда пользователь оставляет ответ на вопрос — автор вопроса\nполучает Telegram-уведомление с ссылкой.\nАнти-спам: не больше 1 уведомления в 10 минут от одного пользователя.\nСтек: Payload CMS hooks + telegram-bot.ts (уже есть).",
        "text",
      ),
      codeBlock(
        "Реализуй поиск по тегам.\nТребования:\n- Фильтрация по нескольким тегам одновременно (AND логика)\n- URL: /tags?q=react,typescript\n- Отображение: та же лента что и главная\n- SEO: canonical URL, h1 с тегами",
        "text",
      ),

      hr(),
      heading(2, "Пример маркеров прогресса"),
      codeBlock(
        "━━━ 🎯 РЕЖИМ: Feature ━━━\nЗадача: Система уведомлений\nОценка: ~2 часа\n\n━━━ 📋 ФАЗА 2: ПЛАН ━━━\nБлоков: 3 | Задач: 8 | Оценка: ~2ч\n  Блок 1: Payload hook при создании Answer\n  Блок 2: Дедупликация уведомлений\n  Блок 3: Telegram-сообщение с форматированием\n⏸️ Жду одобрения плана\n\n━━━ 🔨 ФАЗА 3: ВЫПОЛНЕНИЕ ━━━\nБлок 1/3: Payload hook при создании Answer\n\n━━━ ✅ ЧЕКПОИНТ: Блок 1 завершён ━━━\nСделано: afterOperation hook в Answers коллекции\nСледующий: Блок 2 — дедупликация\n⏸️ Продолжаем?",
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph(
        "Запускай скилл через slash-команду или описание задачи в стиле conventional commits:",
      ),
      codeBlock(
        "# Явный запуск:\n/feature-execution\n\n# Неявный (Claude определяет автоматически):\nfeat: [описание задачи]  # → Feature режим\nfix: [описание бага]     # → Quick Fix режим\nepic: [описание эпика]   # → Epic режим",
        "text",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Как отменить блок и переделать?"),
      paragraph(
        'На любом чекпоинте отвечай "переделай блок N" — Claude вернётся и переделает. Если коммит уже был — создаётся `fix:`-коммит поверх.',
      ),
      heading(3, "Что если задача слишком большая для одной сессии?"),
      paragraph(
        'Claude сохраняет прогресс в `CLAUDE.md` секции ПРОГРЕСС. На следующей сессии пишешь "продолжай с блока N" — и он подхватывает.',
      ),
    ]),
    seoTitle: "feature-execution — Разработка фич по шагам | StackOverVibe",
    seoDescription:
      "Скилл feature-execution: plan → execute → test → commit. Вайбкодинг с чекпоинтами, маркерами прогресса и тестами.",
  },

  /* ─────────────────────────────────────────────
     7. code-writing
  ───────────────────────────────────────────── */
  {
    slug: "code-writing",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Попросил Claude написать код — а он придумал функции которых нет, или подключил библиотеки которые не установлены. Этот скилл учит Claude писать код аккуратно: проверять каждую деталь перед тем как писать.",
      ),
      paragraph(
        "Раньше код падал и нужно было разбираться почему. Теперь Claude сначала смотрит что уже есть в проекте, потом пишет — и код работает с первого раза.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **code-writing** — стандарты написания кода для всех проектов: именование, размеры функций и файлов, типизация TypeScript, паттерны избегания дублирования. Claude применяет их автоматически при написании любого кода.",
      ),
      paragraph(
        "Включает anti-mirage check: перед написанием Claude проверяет, что все импорты, функции и переменные окружения существуют. Галлюцинации исключены.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Пишешь новый модуль или компонент",
        "Рефакторишь существующий код",
        "Ревьюишь pull request",
        "Хочешь убедиться, что код соответствует стандартам",
        "Генерируешь boilerplate (хендлеры, сервисы, утилиты)",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude читает связанные файлы перед написанием кода",
          "Грепает usages изменяемых функций — кто их использует?",
          "Проверяет наличие похожего кода — не пишет с нуля если есть переиспользуемое",
          "Пишет код по стандартам: DRY, KISS, YAGNI, функции <50 строк",
          "Выполняет anti-mirage check: все импорты реальны, env vars в .env.example",
          "При необходимости разбивает файлы >500 строк на модули",
        ],
        true,
      ),

      hr(),
      heading(2, "Стандарты кода"),
      codeBlock(
        "// ✅ Хорошо: русские имена, типизация, JSDoc\n/**\n * Получает пользователя по Telegram ID\n * @param телеграмId — числовой ID пользователя\n */\nasync function получитьПользователяПоTg(\n  телеграмId: number\n): Promise<Пользователь | null> {\n  return db.user.findUnique({\n    where: { telegramId: телеграмId },\n  })\n}\n\n// ❌ Плохо: any, magic numbers, нет типов\nfunction getUser(id: any) {\n  return db.query(`SELECT * FROM users WHERE id = \${id} LIMIT 10`)\n}",
        "typescript",
      ),

      hr(),
      heading(2, "Anti-mirage чеклист"),
      paragraph("После написания каждого блока кода Claude молча проверяет:"),
      list([
        "**Импорты** — каждый `import` ссылается на существующий файл",
        "**Функции** — каждая вызываемая функция существует в импортируемом модуле",
        "**Env vars** — каждая `process.env.VAR` есть в `.env.example`",
        "**Зависимости** — каждый пакет указан в `package.json`",
      ]),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Хук автоформатирования срабатывает после каждого Write/Edit:"),
      codeBlock(
        '// .claude/settings.json\n{\n  "hooks": {\n    "PostToolUse": [{\n      "matcher": "Write|Edit",\n      "hooks": [{\n        "type": "command",\n        "command": "npx prettier --write $FILE 2>/dev/null || true"\n      }]\n    }]\n  }\n}',
        "json",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Нужно ли всегда писать на русском?"),
      paragraph(
        "Да, для этого проекта: переменные, комментарии, docstrings — на русском. Исключение: технические термины (API, URL, JWT), внешние библиотеки и их методы.",
      ),
      heading(3, "Как Claude узнаёт о стандартах?"),
      paragraph(
        "Из `CLAUDE.md` проекта и глобальных правил в `~/.claude/CLAUDE.md`. Чем подробнее описаны правила — тем лучше соблюдаются.",
      ),
    ]),
    seoTitle: "code-writing — Стандарты кода с Claude Code | StackOverVibe",
    seoDescription:
      "Скилл code-writing: DRY, KISS, типизация TS, anti-mirage check. Пишешь код один раз — он работает с первого раза.",
  },

  /* ─────────────────────────────────────────────
     8. testing
  ───────────────────────────────────────────── */
  {
    slug: "testing",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Сделал изменение в коде — и что-то сломалось в другом месте. Тесты — это как охранники: они автоматически проверяют что всё работает после каждого изменения. Этот скилл заставляет Claude писать эти проверки сразу.",
      ),
      paragraph(
        "Ты добавил новую функцию — Claude сам придумает и запустит проверки. Если что-то сломается в будущем, тесты сразу покажут где именно.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **testing** — полное тестирование проекта: unit, integration, E2E. Claude выбирает правильный уровень тестов для каждой задачи по пирамиде тестирования, пишет реальные тесты (не болванки), запускает и фиксит фейлы.",
      ),
      paragraph(
        'Тесты пишутся **сразу в той же сессии**, не откладываются "на потом". Это не опционально.',
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Написал новый модуль с бизнес-логикой",
        "Добавил API-эндпоинт (POST/PUT/DELETE)",
        "Реализовал критичный флоу: авторизация, платежи, регистрация",
        "Рефакторинг — нужно убедиться что ничего не сломалось",
        "CI/CD падает — нужно локально воспроизвести и пофиксить",
      ]),

      hr(),
      heading(2, "Пирамида тестов"),
      paragraph("Claude выбирает уровень по правилам:"),
      list([
        "**Unit** — функция с логикой? Есть if/else, расчёты, трансформация → пишем unit",
        "**Integration** — API-эндпоинт или работа с БД → пишем integration",
        "**E2E** — критичный пользовательский флоу (авторизация, оплата) → пишем E2E",
        "**Smoke** — один раз при инициализации проекта, проверяем что всё стартует",
      ]),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Напиши тесты для функции calculateDiscount в src/lib/pricing.ts.\nПокрой все ветки: скидка 0%, 10%, 20%, невалидный ввод.",
        "text",
      ),
      codeBlock(
        "Напиши integration-тесты для POST /api/questions.\nТестируй: создание с валидными данными, 401 без авторизации,\n400 при невалидном теле запроса, 429 при rate limit.",
        "text",
      ),

      hr(),
      heading(2, "Пример теста"),
      codeBlock(
        "// tests/unit/pricing.test.ts\nimport { описатьСкидку } from '@/lib/pricing'\n\ndescribe('описатьСкидку', () => {\n  it('возвращает 0 для нового пользователя', () => {\n    expect(описатьСкидку({ заказов: 0 })).toBe(0)\n  })\n\n  it('даёт 10% после 5 заказов', () => {\n    expect(описатьСкидку({ заказов: 5 })).toBe(0.1)\n  })\n\n  it('бросает ошибку при отрицательных заказах', () => {\n    expect(() => описатьСкидку({ заказов: -1 })).toThrow(\n      'Количество заказов не может быть отрицательным'\n    )\n  })\n})",
        "typescript",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Запуск тестов в CI при каждом push:"),
      codeBlock(
        '# .github/workflows/test.yml\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - run: npm test -- --coverage\n      - run: npx jest --coverageThreshold=\'{"global":{"lines":80}}\'',
        "yaml",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Сколько моков — это нормально?"),
      paragraph(
        "Правило: если в unit-тесте больше 3 моков — скорее всего нужен integration-тест. Слишком много моков = тест тестирует моки, а не логику.",
      ),
      heading(3, "Нужно ли 100% покрытие?"),
      paragraph(
        "Нет. Целевой показатель — **80% для бизнес-логики**. Геттеры, конфиги, маппинги можно не тестировать. Фокус на тестах, которые реально ловят баги.",
      ),
    ]),
    seoTitle: "testing — Unit, Integration, E2E тесты с Claude | StackOverVibe",
    seoDescription:
      "Скилл testing: пирамида тестов, реальные тесты без болванок, 80% coverage. Пишешь тесты сразу в той же сессии.",
  },

  /* ─────────────────────────────────────────────
     9. api-design
  ───────────────────────────────────────────── */
  {
    slug: "api-design",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Приложение общается с сервером через специальные адреса — это и есть API. Как правильно их назвать и организовать — целая наука. Этот скилл помогает спроектировать всё грамотно с первого раза.",
      ),
      paragraph(
        "Скажи Claude что должно делать твоё приложение — и он придумает как будет устроено общение между фронтом и бэком: какие запросы, какие ответы, как защитить от злоупотреблений.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **api-design** проектирует REST и tRPC API: структуру эндпоинтов, схемы запросов/ответов, коды ошибок, авторизацию, rate limiting. Генерирует OpenAPI-спецификацию и TypeScript-типы.",
      ),
      paragraph(
        'Claude проектирует API с учётом реальных use cases, а не "для красоты". Каждый эндпоинт обоснован, нет лишних ресурсов.',
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Проектируешь новый сервис или микросервис с нуля",
        "Нужно задокументировать существующий API",
        "Добавляешь интеграцию с внешними сервисами (webhook, OAuth)",
        "Хочешь ревью существующего API на соответствие REST conventions",
        "Проектируешь API для мобильного приложения",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude собирает use cases: кто вызывает, с какими данными, что ожидает",
          "Проектирует ресурсы: существительные в URL, глаголы через HTTP-методы",
          "Определяет схемы запросов и ответов через Zod или TypeScript types",
          "Добавляет авторизацию: Bearer JWT, API Key, или session cookies",
          "Настраивает rate limiting: разные лимиты для разных эндпоинтов",
          "Генерирует OpenAPI 3.0 YAML + TypeScript SDK для клиента",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Спроектируй API для системы Q&A:\n- Создание вопроса (авторизованный пользователь)\n- Список вопросов с фильтрацией по тегам и статусу\n- Создание/редактирование ответа\n- Принятие ответа как правильного (только автор вопроса)\n- Реакции (лайк/дизлайк) с дедупликацией\nСтек: Next.js API Routes, Payload CMS, JWT авторизация",
        "text",
      ),

      hr(),
      heading(2, "Пример дизайна эндпоинтов"),
      codeBlock(
        "# Questions API\nGET    /api/questions              # список, ?page=1&tags=react,ts&status=open\nPOST   /api/questions              # создать (auth required)\nGET    /api/questions/:slug        # детали + ответы\nPUT    /api/questions/:id          # редактировать (author only)\nPATCH  /api/questions/:id/close   # закрыть вопрос (author only)\n\n# Answers\nPOST   /api/questions/:id/answers  # ответить (auth)\nPUT    /api/answers/:id            # редактировать (author, 15min window)\nPATCH  /api/answers/:id/accept    # принять ответ (question author)\n\n# Reactions\nPOST   /api/reactions              # лайк/дизлайк (fingerprint dedup)",
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Генерация TypeScript-типов из схем Zod:"),
      codeBlock(
        "// src/lib/api/schemas.ts\nimport { z } from 'zod'\n\nexport const СоздатьВопросSchema = z.object({\n  title: z.string().min(10).max(200),\n  body: z.string().min(30),\n  tags: z.array(z.string()).min(1).max(5),\n})\n\nexport type СоздатьВопросInput = z.infer<typeof СоздатьВопросSchema>\n\n// Использование в route handler:\nconst данные = СоздатьВопросSchema.parse(await req.json())",
        "typescript",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "REST или tRPC — что выбрать?"),
      paragraph(
        "**tRPC** — если фронт и бэк в одном монорепо (Next.js fullstack). Нулевой оверхед, типы из коробки. **REST** — если нужна интеграция с внешними клиентами или мобилкой.",
      ),
      heading(3, "Как версионировать API?"),
      paragraph(
        "Для публичных API: `/api/v1/`, `/api/v2/`. Для внутренних (только свой фронт) — версионирование не нужно, меняй сразу.",
      ),
    ]),
    seoTitle: "api-design — Проектирование REST и tRPC API | StackOverVibe",
    seoDescription:
      "Скилл api-design: эндпоинты, схемы Zod, авторизация, rate limiting, OpenAPI. Проектируй API который работает с первого раза.",
  },

  /* ─────────────────────────────────────────────
     10. frontend-design
  ───────────────────────────────────────────── */
  {
    slug: "frontend-design",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Хочешь красивую кнопку, карточку или целую страницу — но не знаешь как написать код? Этот скилл превращает твоё описание в готовый визуальный компонент. Работает на мобильном, планшете и компьютере.",
      ),
      paragraph(
        "Опиши словами что должно выглядеть и как вести себя — Claude напишет код который сразу работает. Никаких правок в браузере, никаких загадочных ошибок.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **frontend-design** создаёт React/Next.js компоненты с нуля: адаптивную вёрстку, анимации, тёмную тему, доступность (a11y). Claude пишет компоненты которые работают — без правок в браузере.",
      ),
      paragraph(
        "Специализируется на **ретро-хакерском стиле** (стек StackOverVibe): CRT-эффекты, глитч, neon glow, ASCII-арт, моноширинные шрифты.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Нужен новый React-компонент (карточка, форма, модал, таблица)",
        "Вёрстаешь страницу по макету или описанию",
        "Добавляешь анимацию (framer-motion, CSS transitions)",
        "Адаптируешь под мобильные устройства",
        "Ревьюишь компонент на доступность и производительность",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude запрашивает: описание компонента, данные (props), контекст использования",
          "Проверяет существующие компоненты — переиспользует если есть похожее",
          "Пишет TypeScript-интерфейс пропсов с JSDoc-комментариями",
          "Реализует компонент с Tailwind классами (mobile-first)",
          "Добавляет анимации через `framer-motion` или CSS если нужно",
          "Проверяет доступность: aria-атрибуты, keyboard navigation, contrast ratio",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Создай компонент FeedCard для отображения элемента ленты.\nТипы: question, guide, tool, framework.\nДанные: title, description, author, tags, stats (views, likes).\nСтиль: ретро-хакер, тёмная тема, border-green-500/30.\nАнимация: hover — glow эффект на border.",
        "text",
      ),
      codeBlock(
        "Сделай мобильное меню:\n- Кнопка [=] / [x] в хедере\n- Выдвигается сверху вниз\n- Анимация: slide down + fade\n- Закрывается по клику вне меню\n- Блокирует скролл страницы",
        "text",
      ),

      hr(),
      heading(2, "Пример компонента"),
      codeBlock(
        "// components/ui/Badge.tsx\ninterface БейджПропс {\n  /** Текст бейджа */\n  текст: string\n  /** Вариант оформления */\n  вариант?: 'default' | 'success' | 'warning' | 'error'\n  className?: string\n}\n\nexport function Бейдж({ текст, вариант = 'default', className }: БейджПропс) {\n  const варианты = {\n    default: 'border-green-500/30 text-green-400',\n    success: 'border-emerald-500/30 text-emerald-400',\n    warning: 'border-yellow-500/30 text-yellow-400',\n    error: 'border-red-500/30 text-red-400',\n  }\n  return (\n    <span className={`border px-2 py-0.5 text-xs font-mono \${варианты[вариант]} \${className}`}>\n      {текст}\n    </span>\n  )\n}",
        "typescript",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Storybook для изолированной разработки компонентов:"),
      codeBlock(
        '# Запуск Storybook локально\nnpx storybook dev -p 6006\n\n# Генерация story для компонента через Claude:\n"Создай Storybook story для компонента Бейдж.\nПокажи все 4 варианта: default, success, warning, error."',
        "bash",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Как получить ретро-хакерский стиль?"),
      paragraph(
        'В промпте укажи: "стиль ретро-хакер 80-х: тёмный фон #0a0a0a, зелёный текст #22c55e, моноширинный шрифт JetBrains Mono, CRT-скенлайны, border-green-500/30". Claude применит всё автоматически.',
      ),
      heading(3, "Как проверить доступность (a11y)?"),
      paragraph(
        "Claude добавляет `aria-label`, `role`, `tabIndex` автоматически. Для проверки запусти `npx axe-cli http://localhost:3000` или Chrome DevTools Accessibility tree.",
      ),
    ]),
    seoTitle: "frontend-design — React компоненты, ретро-стиль | StackOverVibe",
    seoDescription:
      "Скилл frontend-design: Next.js, Tailwind, framer-motion, тёмная тема, CRT-эффекты. Компоненты которые работают с первого раза.",
  },

  /* ─────────────────────────────────────────────
     11. interface-design
  ───────────────────────────────────────────── */
  {
    slug: "interface-design",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Перед тем как рисовать интерфейс, надо понять как им будут пользоваться. Этот скилл помогает продумать весь путь пользователя — от входа до результата — ещё до написания кода.",
      ),
      paragraph(
        "Claude задаёт правильные вопросы и рисует схему текстом: где что находится, как переходить между экранами. Ты видишь результат заранее и можешь поправить до того как написан хоть один файл.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **interface-design** проектирует пользовательские интерфейсы на уровне концепции: информационную архитектуру, user flows, wireframes в ASCII, описание компонентов и взаимодействий. Это шаг ДО написания кода.",
      ),
      paragraph(
        "Claude выступает UX-дизайнером: задаёт правильные вопросы про аудиторию, цели, сценарии использования — и только потом предлагает решение.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Начинаешь новый экран или раздел — нужна концепция",
        'Есть задача "сделай удобно" без конкретного макета',
        "Нужно согласовать UI до начала вёрстки",
        "Хочешь проверить user flow — все ли сценарии покрыты",
        "Проектируешь форму, wizard или сложный интерактивный элемент",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude задаёт вопросы: кто пользователь, какова цель, какой контекст использования",
          "Строит user flow: шаги пользователя от входа до результата",
          "Рисует ASCII-wireframe: расположение элементов, иерархия",
          "Описывает компоненты: что показывает, какие состояния (empty, loading, error)",
          "Указывает micro-interactions: что происходит при hover, click, validation",
          "Предлагает альтернативные варианты (A/B) если есть неопределённость",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Спроектируй страницу /questions/ask.\nПользователь: разработчик с вопросом про код.\nЦель: задать вопрос быстро, без лишних полей.\nОбязательно: заголовок, тело (markdown), теги.\nОпционально: ссылка на репо/коддсандбокс.",
        "text",
      ),
      codeBlock(
        "Нужен дизайн уведомлений в хедере.\nТипы уведомлений: ответ на вопрос, принятый ответ, реакция.\nПоведение: badge с количеством, dropdown при клике, отметить прочитанным.\nМобильная версия: отдельная страница /notifications.",
        "text",
      ),

      hr(),
      heading(2, "Пример ASCII-wireframe"),
      codeBlock(
        "┌─────────────────────────────────────────────┐\n│  [≡] StackOverVibe        [🔍] [Login]       │\n├─────────────────────────────────────────────┤\n│                                             │\n│  Задай вопрос                               │\n│  ─────────────────────────────────────────  │\n│  Заголовок *                                │\n│  ┌───────────────────────────────────────┐  │\n│  │ Как сделать X с помощью Y?            │  │\n│  └───────────────────────────────────────┘  │\n│  Подсказка: Сформулируй кратко и конкретно  │\n│                                             │\n│  Описание проблемы *                        │\n│  ┌───────────────────────────────────────┐  │\n│  │ [markdown editor]                     │  │\n│  │                                       │  │\n│  └───────────────────────────────────────┘  │\n│                                             │\n│  Теги *  [react] [typescript] [+добавить]   │\n│                                             │\n│  [Предпросмотр]         [Опубликовать →]    │\n└─────────────────────────────────────────────┘",
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("После согласования дизайна — переход к вёрстке:"),
      codeBlock(
        '# Шаг 1: Дизайн (этот скилл)\n"Спроектируй страницу /questions/ask"\n\n# Шаг 2: После одобрения wireframe — вёрстка\n"Отлично, верстай по этому wireframe.\nСтек: Next.js + Tailwind + ретро-хакер стиль."',
        "text",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Нужно ли согласовывать каждый экран?"),
      paragraph(
        "Для простых экранов (список, детальная страница) — нет, сразу кодь. Для сложных взаимодействий (wizard, drag-and-drop, форма с условиями) — сначала wireframe.",
      ),
      heading(3, "Как описать дизайн мобильной версии?"),
      paragraph(
        "Попроси Claude нарисовать второй wireframe с шириной 375px. Он покажет, как элементы перестроятся: стек вместо сетки, нижняя навигация вместо хедера.",
      ),
    ]),
    seoTitle: "interface-design — UX и wireframes с Claude | StackOverVibe",
    seoDescription:
      "Скилл interface-design: user flows, ASCII wireframes, компоненты, micro-interactions. Проектируй UI до кода.",
  },

  /* ─────────────────────────────────────────────
     12. security-auditor
  ───────────────────────────────────────────── */
  {
    slug: "security-auditor",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Сделал сайт — но как проверить что его нельзя взломать? Этот скилл как охранник: проверяет весь код на дыры в безопасности и находит места где злоумышленник мог бы навредить.",
      ),
      paragraph(
        "Claude читает твой код, находит уязвимые места и сразу исправляет их. Ты получаешь отчёт: что было, что исправлено, что нужно сделать позже.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **security-auditor** проводит аудит безопасности кода: SQL-инъекции, XSS, CSRF, захардкоженные секреты, незащищённые эндпоинты, уязвимые зависимости. Выдаёт отчёт с приоритетами и патчами.",
      ),
      paragraph(
        "Автоматически подключается при написании кода, связанного с авторизацией, платежами и пользовательским вводом.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Перед деплоем новой фичи с пользовательским вводом",
        "После добавления API-эндпоинтов (особенно POST/PUT/DELETE)",
        "При интеграции платёжного шлюза",
        "Периодический аудит (раз в месяц или при релизе)",
        "После подозрительного поведения в логах",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude читает целевые файлы: хендлеры, middleware, работу с БД",
          "Ищет паттерны уязвимостей: конкатенация в SQL, `innerHTML`, `eval()`, прямой доступ к `process.env`",
          "Проверяет `package.json` через `npm audit` — критические уязвимости в зависимостях",
          "Проверяет авторизацию: все ли эндпоинты проверяют права?",
          "Составляет отчёт: Critical → Major → Minor с конкретными патчами",
          "Применяет патчи молча, показывает только финальный отчёт",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Проведи security-аудит файлов src/app/api/\nОсобое внимание: авторизация, работа с пользовательскими данными.",
        "text",
      ),
      codeBlock(
        "/security-auditor\nПроверь весь проект перед деплоем на прод.\nВыдай отчёт: Critical, Major, Minor.\nИсправь Critical и Major автоматически.",
        "text",
      ),

      hr(),
      heading(2, "Пример отчёта"),
      codeBlock(
        "━━━ SECURITY AUDIT ━━━\n\n🔴 Critical (1):\n  src/app/api/users/route.ts:45\n  SQL-конкатенация: WHERE id = \${params.id}\n  → Заменено на параметризованный запрос\n\n🟡 Major (2):\n  src/app/api/questions/route.ts — нет rate limiting\n  → Добавлен: 10 req/min per IP\n\n  .env.example не содержит TELEGRAM_BOT_TOKEN\n  → Добавлено с описанием как получить\n\n🟢 Minor (1):\n  src/lib/auth.ts — JWT не проверяет audience (aud)\n  → Техдолг, добавить в следующем спринте\n\nИтого: 1 Critical (исправлен), 2 Major (исправлены), 1 Minor (отложен)",
        "text",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Запуск аудита зависимостей в CI:"),
      codeBlock(
        "# .github/workflows/security.yml\njobs:\n  audit:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm audit --audit-level=high\n      - run: npx better-npm-audit audit --level high",
        "yaml",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Насколько полный аудит делает скилл?"),
      paragraph(
        "Скилл покрывает **OWASP Top 10** для Node.js: инъекции, broken auth, exposure, XXE, broken access control, security misconfiguration, XSS, insecure deserialization, known vulnerabilities, insufficient logging. Пентест заменить не может.",
      ),
      heading(3, "Что делать если Critical нельзя быстро починить?"),
      paragraph(
        "Добавить **временный workaround** (например, whitelist на эндпоинте) и создать issue с приоритетом P0. Не деплоить с нераспределёнными Critical.",
      ),
    ]),
    seoTitle:
      "security-auditor — Аудит безопасности кода с Claude | StackOverVibe",
    seoDescription:
      "Скилл security-auditor: SQL-инъекции, XSS, OWASP Top 10, npm audit. Аудит и автоматическое исправление уязвимостей.",
  },

  /* ─────────────────────────────────────────────
     13. backlog-management
  ───────────────────────────────────────────── */
  {
    slug: "backlog-management",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Накопилась куча идей и непонятно с чего начать? Этот скилл помогает превратить хаос в чёткий список задач с приоритетами. Как блокнот, только умный: сам считает что важнее всего делать прямо сейчас.",
      ),
      paragraph(
        "Расскажи что хочешь добавить в проект — Claude разобьёт это на конкретные шаги, расставит приоритеты и запишет в файл. В следующий раз просто откроешь список и сразу знаешь что делать.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **backlog-management** ведёт бэклог задач в формате `docs/tasks.md` и `docs/tasks-detailed.md`. Принимает сырые идеи, превращает в атомарные задачи с приоритетами, декомпозирует эпики, обновляет статусы.",
      ),
      paragraph(
        "Всё живёт в markdown-файлах в репозитории — никаких внешних систем, никакого Jira.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Накопилось много идей и непонятно с чего начать",
        "Нужно декомпозировать эпик на конкретные задачи",
        "Хочешь расставить приоритеты по RICE или MoSCoW",
        "После ретроспективы — записать что делать по-другому",
        "Переоцениваешь бэклог после изменения приоритетов",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude читает текущий `tasks.md` — что уже есть, что сделано",
          'Принимает новые идеи в свободной форме: "хочу добавить X"',
          "Декомпозирует на атомарные задачи (каждая выполнима за 1-4 часа)",
          "Расставляет приоритеты через **RICE**: Reach × Impact × Confidence / Effort",
          'Добавляет зависимости: "Задача 3 требует выполнения задачи 1"',
          "Обновляет `tasks-detailed.md` — технические детали каждой задачи",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Добавь в бэклог:\n- Уведомления в Telegram при новом ответе\n- Экспорт вопросов в PDF\n- Поиск по полному тексту (не только заголовки)\n- Тёмная/светлая тема по системному предпочтению\nРасставь приоритеты, декомпозируй.",
        "text",
      ),
      codeBlock(
        "Обнови бэклог после сессии:\nСделано: задачи 7.1, 7.2, 7.3\nНе делаем: экспорт в PDF (низкий RICE)\nДобавляем: задача из issue #47 — фикс пагинации",
        "text",
      ),

      hr(),
      heading(2, "Формат tasks.md"),
      codeBlock(
        "# Бэклог StackOverVibe\n\n## В работе\n- [ ] 7.4 Feed API — агрегация всех типов контента\n  _Зависит от: 7.1, 7.2, 7.3_\n\n## Готово к старту (приоритет)\n- [ ] 7.5 Tags расширены — description, SEO-поля\n- [ ] 7.6 Reactions/Comments — типы question/answer\n\n## Бэклог\n- [ ] 8.1 Telegram-уведомления (RICE: 72)\n- [ ] 8.2 Полнотекстовый поиск (RICE: 45)\n\n## Отложено\n- [ ] Экспорт в PDF (RICE: 12, низкий приоритет)",
        "markdown",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph("Синхронизация задач из GitHub Issues в `tasks.md`:"),
      codeBlock(
        "# Получить открытые issues и добавить в tasks.md\ngh issue list --json title,number,labels \\\n  | jq '.[] | \"- [ ] #\\(.number) \\(.title)\"' \\\n  >> docs/tasks.md",
        "bash",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Как считать RICE?"),
      paragraph(
        "**Reach** — сколько пользователей затронет фича за месяц (0-100). **Impact** — насколько улучшит их опыт (0.25/0.5/1/2/3). **Confidence** — насколько уверены в оценках (0.5/0.8/1.0). **Effort** — человеко-дни. Итого: R×I×C / E.",
      ),
      heading(3, "Можно ли использовать GitHub Projects вместо tasks.md?"),
      paragraph(
        "Можно, но tasks.md преимущества: **рядом с кодом**, Claude читает напрямую без API, работает в offline, версионируется в git.",
      ),
    ]),
    seoTitle:
      "backlog-management — Бэклог задач в markdown с Claude | StackOverVibe",
    seoDescription:
      "Скилл backlog-management: tasks.md, RICE-приоритизация, декомпозиция эпиков. Никакого Jira — всё в репозитории.",
  },

  /* ─────────────────────────────────────────────
     14. docker-deploy
  ───────────────────────────────────────────── */
  {
    slug: "docker-deploy",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Сделал сайт локально — теперь надо выложить в интернет. Звучит страшно, но этот скилл делает всё за тебя. Docker — это как коробка, в которую упакован твой сайт. Её можно перенести на любой сервер и он заработает.",
      ),
      paragraph(
        "Скажи Claude адрес сервера и домен — и через несколько минут сайт будет доступен по ссылке с защищённым соединением. Если что-то сломается — один клик и всё вернётся как было.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **docker-deploy** настраивает Docker-деплой для любого стека: пишет `Dockerfile`, `docker-compose.yml`, настраивает nginx, SSL через certbot, systemd-автозапуск и GitHub Actions для CI/CD.",
      ),
      paragraph(
        "Один промпт — и у тебя полная цепочка: push → build → deploy → healthy. Rollback за 30 секунд.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Деплоишь проект на VPS впервые",
        "Переносишь проект с одного сервера на другой",
        "Настраиваешь CI/CD pipeline через GitHub Actions",
        "Нужен zero-downtime деплой через `docker compose up --no-deps --build`",
        "Настраиваешь multi-container app (app + postgres + redis + nginx)",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Claude анализирует стек: Node.js/Python/Go — выбирает правильный базовый образ",
          "Пишет многоступенчатый `Dockerfile`: builder + runner для минимального размера",
          "Настраивает `docker-compose.yml`: app + зависимости (postgres, redis)",
          "Конфигурирует nginx как reverse proxy с HTTP→HTTPS редиректом",
          "Настраивает certbot для автоматического получения и обновления SSL",
          "Добавляет healthcheck в `docker-compose.yml` для мониторинга",
          "Пишет GitHub Actions workflow: push → SSH → pull → build → up",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Настрой Docker-деплой для Next.js 14 + Payload CMS проекта.\nСервер: Ubuntu 22.04, IP: 109.172.36.108\nДомен: stackovervibe.ru\nБД: PostgreSQL 15 (в отдельном контейнере)\nSSL: Let's Encrypt\nCI/CD: GitHub Actions → SSH-деплой",
        "text",
      ),
      codeBlock(
        "Обнови docker-compose.yml — добавь Redis для кэширования.\nredis:7-alpine, порт 6379, данные в volume.\nApp должен ждать redis (depends_on + healthcheck).",
        "text",
      ),

      hr(),
      heading(2, "Пример Dockerfile"),
      codeBlock(
        '# Dockerfile — многоступенчатая сборка Next.js\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine AS runner\nWORKDIR /app\nENV NODE_ENV=production\nCOPY --from=builder /app/.next/standalone ./\nCOPY --from=builder /app/.next/static ./.next/static\nCOPY --from=builder /app/public ./public\nEXPOSE 3000\nCMD ["node", "server.js"]',
        "text",
      ),

      hr(),
      heading(2, "Автоматизация CI/CD"),
      codeBlock(
        '# .github/workflows/deploy.yml\njobs:\n  deploy:\n    runs-on: self-hosted\n    steps:\n      - uses: actions/checkout@v4\n      - name: Deploy\n        run: |\n          ssh deploy@109.172.36.108 \\\n            "cd /home/claude/stackovervibe && \\\n             git pull && \\\n             docker compose up -d --build --no-deps app && \\\n             docker compose exec app npx prisma migrate deploy"',
        "yaml",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Как откатить деплой если что-то сломалось?"),
      paragraph(
        '`git revert HEAD && git push` — запустит CI который задеплоит предыдущую версию. Или вручную: `ssh server "cd /app && git checkout HEAD~1 && docker compose up -d --build"`.',
      ),
      heading(3, "Как проверить что деплой прошёл успешно?"),
      paragraph(
        "Claude добавляет healthcheck: `curl -f http://localhost:3000/api/health` в `docker-compose.yml`. Если контейнер не отвечает за 30с — Docker перезапустит его автоматически.",
      ),
    ]),
    seoTitle: "docker-deploy — Docker деплой на VPS с нуля | StackOverVibe",
    seoDescription:
      "Скилл docker-deploy: Dockerfile, docker-compose, nginx, SSL, GitHub Actions CI/CD. Push → deploy за один раз.",
  },

  /* ─────────────────────────────────────────────
     15. vps-quick-setup
  ───────────────────────────────────────────── */
  {
    slug: "vps-quick-setup",
    description: root([
      heading(2, "Простым языком"),
      paragraph(
        "Купил сервер — и перед тобой пустая машина. Нужно настроить безопасность, установить нужные программы и подготовить место для сайтов. Раньше это занимало день и кучу гугла. Этот скилл делает всё за 15 минут.",
      ),
      paragraph(
        "Дай Claude адрес сервера и свой ключ для входа — он сам настроит защиту, установит всё нужное и подготовит сервер к работе. Потом просто деплоишь проекты.",
      ),
      hr(),
      heading(2, "Что делает"),
      paragraph(
        "Скилл **vps-quick-setup** настраивает свежий VPS с нуля: создаёт пользователя, настраивает SSH-ключи, firewall, fail2ban, устанавливает Docker, nginx, certbot. Сервер готов к деплою за 15 минут.",
      ),
      paragraph(
        "Один промпт вместо часа поиска гайдов и бесконечного copy-paste из StackOverflow.",
      ),

      hr(),
      heading(2, "Когда использовать"),
      list([
        "Новый VPS только что куплен (Beget, DigitalOcean, Hetzner, etc.)",
        "Переустановка ОС на существующем сервере",
        "Настройка тестового сервера для нового проекта",
        "Клонирование конфигурации с одного сервера на другой",
        "После взлома — полная пересборка с нуля",
      ]),

      hr(),
      heading(2, "Как работает — пошагово"),
      list(
        [
          "Создаёт пользователя `deploy` без root-доступа, добавляет в sudoers",
          "Настраивает SSH: копирует публичный ключ, отключает password auth и root login",
          "Настраивает UFW firewall: разрешает 22/SSH, 80/HTTP, 443/HTTPS — всё остальное блокирует",
          "Устанавливает fail2ban: блокирует IP после 3 неудачных попыток SSH",
          "Устанавливает Docker + Docker Compose v2 через официальный скрипт",
          "Устанавливает nginx + certbot (snap) для SSL",
          "Настраивает автообновление certbot через cron",
          "Создаёт структуру директорий под проекты: `~/projects/`, `~/backups/`",
        ],
        true,
      ),

      hr(),
      heading(2, "Промпты для Claude"),
      codeBlock(
        "Настрой новый VPS:\nIP: 109.172.36.108\nОС: Ubuntu 22.04\nПользователь деплоя: claude\nМой публичный ключ SSH: ssh-ed25519 AAAA... ivan@mac\nПроекты: stackovervibe (порт 3000), один домен stackovervibe.ru",
        "text",
      ),
      codeBlock(
        "Добавь на существующий сервер:\n- Второй проект на порту 3001\n- nginx upstream для нового проекта\n- SSL для домена newproject.ru",
        "text",
      ),

      hr(),
      heading(2, "Пример скрипта настройки"),
      codeBlock(
        "#!/bin/bash\n# setup-vps.sh — запускается от root на свежем сервере\nset -e\n\n# Создаём пользователя\nuseradd -m -s /bin/bash claude\nusermod -aG sudo claude\n\n# SSH-ключ\nmkdir -p /home/claude/.ssh\necho \"ssh-ed25519 AAAA... ivan@mac\" >> /home/claude/.ssh/authorized_keys\nchmod 700 /home/claude/.ssh\nchmod 600 /home/claude/.ssh/authorized_keys\nchown -R claude:claude /home/claude/.ssh\n\n# Отключаем password auth\nsed -i 's/#PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config\nsed -i 's/PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config\nsystemctl restart sshd\n\n# Firewall\nufw default deny incoming\nufw allow ssh\nufw allow http\nufw allow https\nufw --force enable\n\necho \"✅ VPS готов к деплою\"",
        "bash",
      ),

      hr(),
      heading(2, "Автоматизация"),
      paragraph(
        "Сохрани скрипт настройки в приватный репозиторий для повторного использования:",
      ),
      codeBlock(
        '# Быстрая настройка нового сервера\nssh root@NEW_IP "bash -s" < ~/setup-vps.sh\n\n# Или через curl из приватного gist\nssh root@NEW_IP "curl -s https://gist.../setup.sh | bash"',
        "bash",
      ),

      hr(),
      heading(2, "Частые вопросы"),
      heading(3, "Нужен ли root для первоначальной настройки?"),
      paragraph(
        "Да, только для первого подключения. После настройки пользователя `claude` и отключения root-логина — работаешь только через `claude`. `sudo` доступен для административных задач.",
      ),
      heading(3, "Как добавить второй домен на тот же сервер?"),
      paragraph(
        "Claude создаёт отдельный nginx конфиг в `/etc/nginx/sites-available/newdomain.ru` и получает SSL через `certbot --nginx -d newdomain.ru`. Порты 80/443 один nginx, разные upstream по доменам.",
      ),
    ]),
    seoTitle:
      "vps-quick-setup — Настройка VPS с нуля за 15 минут | StackOverVibe",
    seoDescription:
      "Скилл vps-quick-setup: SSH, UFW, fail2ban, Docker, nginx, SSL. Готовый к деплою VPS за один промпт.",
  },
];
